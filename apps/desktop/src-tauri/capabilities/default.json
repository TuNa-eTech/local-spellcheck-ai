{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "Only core Tauri window/event APIs are exposed. Filesystem and processes stay behind typed Rust commands.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
